# Deployment

- Host the Next.js app on Vercel or another Node-compatible platform.
- Use Supabase/Postgres for production data.
- Configure Stripe webhooks at `/api/webhooks/stripe`.
- Store secrets in platform environment variables.
- Enable automated backups for Postgres and product media.
